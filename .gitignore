Nothing should be ignored based on the provided file changes. The only added file is `penpot-projects-strict.html`, which appears to be a source/config file (HTML document) and not a build artifact, dependency, or temporary file.